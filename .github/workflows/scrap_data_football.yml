name: Scrap of data

on:
  workflow_dispatch:
  #schedule:
    # * is a special character in YAML so you have to quote this string
    # Executar todas segundas-feiras (1) e quintas-feiras (4) as 9h e 18h
    #- cron:  '5 12,21 * * 1,4'
jobs:
  scrap_football_data:

    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: "3.9"
    - name: Install lib pandas
      run: |
        python -m pip install --upgrade pip
        pip install pandas
    - name: Install lib requests
      run: pip install requests
      
    - name: Install lib bs4
      run: pip install bs4
      
    - name: do_scrap_football
      run: |
          pwd;ls -l;cd SCRIPT;python scrap_football_data.py
          ls -l

    - name: check if csv was created
      run: |
          pwd;cd SCRIPT; touch teste_script; ls -l ; cd ../CSV; pwd; touch csv; ls -l
          
    - name: Checkout Repository
      uses: actions/checkout@v3 
      with:
        persist-credentials: false
        repository: ${{ github.event.pull_request.head.repo.fut_serieA_2023 }}
        ref: ${{ github.event.pull_request.head.ref }}

    - name: check if csv was created
      run: |
          pwd;ls -l SCRIPT; ls -l CSV


    # - name: Checkout
    #   uses: actions/checkout@master
    #   # Repo github
    # - name: Upload to github
    #   uses: actions/github-upload-action@v0.1.0
    #   with:
    #       access-token: ${{ secrets.ACCESS_TOKEN }}
    #       file-path: CSV
    #       owner: silviolima07
    #       repo: fut_serieA_2023
    #       remote-dir: CSV
    # - name: Teste com upload artifactv3
        
    #   uses: actions/upload-artifact@v3
    #   with:
    #       name: dados_2012_2023.csv
    #       path: CSV

    # - uses: actions/upload-artifact@v1
    #   with:
    #      name: dados_2012_2023.csv
    #      path: CSV 
         
    # - name: teste download 1
    #   uses: actions/download-artifact@v3
    #   with:
    #       name: dados_2012_2023.csv
    #       path: .

    # - name: Download a single artifact
    #   uses: actions/download-artifact@v3
    #   with:
    #     name: dados_2012_2023.csv
    #     path: CSV   

    - name: Commit files
      run: |
        git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        git commit -a -m "Add changes"
        pwd;ls -l
        
    - name: Push changes
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        branch: ${{ github.ref }}     

    


          
        
